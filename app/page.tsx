import Hero from "@/components/Hero";
import Yield from "@/components/Yield";

const HomePage = () => {
  return (
    <main className="m-4">
      <Hero/>
      <Yield/>
    </main>
  );
}

export default HomePage;